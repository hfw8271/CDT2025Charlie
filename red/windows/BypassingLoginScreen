#methods to bypass login screen:

#Sticky Keys Exploit (Local Admin Bypass) (If back door was set up beforehand)
#1. Press SHIFT key 5 times at the login screen.
#2. If the exploit is in place, a SYSTEM-level command prompt (cmd.exe) will open.
#3. Reset the password:         
#     net user <username> <newpassword>
#4. Log in with the new password.


#Boot into Safe Mode with Command Prompt
#1. Reboot and interrupt the startup three to trigger Advance Startup options
#2. Select Startup Settings -> Enable Safe Mode with Command Prompt.
#3. If a command prompt appears instead of the login screen:
#     net user Administrator <newpassword>
#4. Restart and log in with the new password.
